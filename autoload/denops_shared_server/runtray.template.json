{
    "name": "denops-shared-server",
    "description": "denops.vim shared server",
    "executable": "{{deno}}",
    "arguments": [
        "run",
        "{{deno_args}}",
        "{{script}}",
        "--hostname",
        "{{hostname}}",
        "--port",
        "{{port}}"
    ]
}
