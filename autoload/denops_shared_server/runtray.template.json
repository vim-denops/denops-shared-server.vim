{
    "name": "denops-shared-server",
    "description": "denops.vim shared server",
    "executable": "{{deno}}",
    "arguments": [
        "run", "-A", "--no-check", "--unstable", "{{script}}",
        "--hostname", "{{hostname}}",
        "--port", "{{port}}"
    ]
}
